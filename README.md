# 🎬 Kinorium WebScraper

**Kinorium WebScraper** — це API-сервіс для автоматизованого збору даних про фільми з платформи [Kinorium](https://ua.kinorium.com).

Проєкт побудований на базі **FastAPI** та підтримує кілька режимів парсингу — від швидких HTTP-запитів до повноцінної емуляції браузера через **Playwright**.

---

## 🚀 Основні можливості

Сервіс підтримує три режими роботи (`mode`):

### ⚡ HTTP Mode (`http`)
- Швидкий пошук списків фільмів за жанрами.
- Працює через звичайні HTTP-запити.
- **Переваги:** Максимальна швидкість, мінімальні ресурси.

### 🧠 Headless Mode (`headless`)
- Збирає детальну інформацію про фільм:
    - рейтинг
    - актори
    - опис
    - тривалість
- Використовує Playwright (Chromium).
- Підтримує динамічний контент.

### 🖥 UI Mode (`ui`)
- Автоматично відкриває сторінку фільму у браузері.
- **Важливо:** Працює лише при локальному запуску.
- У Docker повертає посилання для ручного відкриття.

---

## 🐳 Швидкий запуск (Docker)

### 1. Завантаження образу
```bash
docker pull dmytrosydor/webscraper:latest
```
### 2. Запуск контейнера
```bash
docker run -d -p 8000:8000 --name my-app-instance dmytrosydor/webscraper:latest
```
Сервіс буде доступний за адресою: [http://localhost:8000](http://localhost:8000)

---

## 🧪 Тестування

### Варіант 1 — Postman
У папці `postman_collection` знаходиться готова колекція запитів.
* **Імпорт:** Postman → File → Import → обрати `.json` файл.

### Варіант 2 — Python client (Docker образ повинен бути запущений)
Оскільки Docker не може відкривати вікно браузера, для тестування 3 режиму ( відкриття вікна у браузері ) використовується клієнтський скрипт `client_test.py`.

#### Налаштування середовища 
- Версія не має великого значення як у прикладі з локальним запуском 
```
python -m venv .venv
```
#### Активація
**Windows:**
```
.venv\Scripts\activate
```
**Mac / Linux:**
```
source .venv/bin/activate
```
#### Встановлення requests та запуск
```
pip install requests
python client_test.py
```
---

## 🛠 Локальний запуск (без Docker)

### Вимоги
- Python 3.12.x (рекомендовано **3.12.10**)

### Налаштування
```
python3.12 -m venv .venv
```
### Активація
**Windows:**
```
.venv\Scripts\activate
```
**Mac / Linux:**
```
source .venv/bin/activate
```
### Встановлення та запуск
```
pip install -r requirements.txt
uvicorn src.main:app
```
📄 **FasApi Docs:** [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)

**Postman** У папці `postman_collection` знаходиться готова колекція запитів.
* **Імпорт:** Postman → File → Import → обрати `.json` файл.

---

## ⚙️ Параметр mode

Доступні значення:
- `http`
- `headless`
- `ui`

---

## ✅ Підсумок

- 🚀 Швидкий HTTP-парсинг
- 🧠 Headless-аналітика через Playwright
- 🖥 UI-режим для локальної взаємодії
- 🐳 Повна підтримка Docker
- 🧪 Зручне тестування через Postman або Python client
